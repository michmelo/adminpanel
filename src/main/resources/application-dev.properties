# -------------------
# Configuración del perfil dev
# -------------------

# Conexión a la base de datos (MySQL local de ejemplo)
spring.datasource.url=jdbc:mysql://44.217.21.127:3306/VitalConnect?useSSL=false&serverTimezone=UTC
spring.datasource.username=admin
spring.datasource.password=DB_ADMIN

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Puerto del servidor (opcional)
server.port=8082

# Devtools (opcional)
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true
